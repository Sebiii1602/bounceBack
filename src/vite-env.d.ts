/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Wird von Vite zur Bauzeit ersetzt — siehe `define` in vite.config.ts. */
declare const __APP_BUILD__: { commit: string; date: string }
